{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ramen-schema.org/shoyu/2026.1/annotations.schema.json",
  "title": "Shoyu Annotations",
  "description": "Configuration for resolving and rendering text annotations in Shoyu text views.",
  "type": "object",
  "additionalProperties": false,
  "required": ["definitions"],
  "properties": {
    "definitions": {
      "type": "object",
      "description": "Maps annotation type names, for example TEI element names or derived annotation keys, to render definitions.",
      "additionalProperties": {
        "$ref": "#/$defs/annotationDefinition"
      }
    },
    "unknownAnnotation": {
      "$ref": "#/$defs/unknownAnnotationStrategy",
      "description": "Strategy used when an annotation type has no matching definition.",
      "default": "warn"
    },
    "invalidRange": {
      "$ref": "#/$defs/invalidRangeStrategy",
      "description": "Strategy used when an annotation range is invalid or outside the rendered text.",
      "default": "warn"
    }
  },
  "$defs": {
    "annotationDefinition": {
      "description": "Render definition for one annotation type.",
      "oneOf": [
        {
          "$ref": "#/$defs/inlineDefinition"
        },
        {
          "$ref": "#/$defs/structureDefinition"
        }
      ]
    },
    "inlineDefinition": {
      "type": "object",
      "description": "Definition for annotations rendered in or detached from the text flow.",
      "additionalProperties": false,
      "required": ["layer"],
      "properties": {
        "layer": {
          "const": "inline",
          "description": "Declares this definition as an inline annotation."
        },
        "behavior": {
          "$ref": "#/$defs/inlineBehavior",
          "description": "Rendering behavior for this inline annotation.",
          "default": "mark"
        },
        "renderAs": {
          "$ref": "#/$defs/inlineRenderElement",
          "description": "HTML element used to render in-place inline annotations.",
          "default": "span"
        },
        "placement": {
          "$ref": "#/$defs/annotationPlacement",
          "description": "Placement used when an annotation is rendered as detached content.",
          "default": "inline"
        },
        "priority": {
          "type": "integer",
          "description": "Ordering hint for nested inline annotations. Lower values wrap outside higher values."
        },
        "classes": {
          "type": "array",
          "description": "CSS utility classes added to the rendered annotation element.",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "classMappings": {
          "type": "array",
          "description": "Rules that map annotation property values to CSS classes.",
          "items": {
            "$ref": "#/$defs/annotationClassMapping"
          },
          "default": []
        },
        "label": {
          "type": "array",
          "description": "Bindings whose resolved values are joined to form the visible label for detached annotations.",
          "items": {
            "$ref": "#/$defs/annotationValue"
          },
          "default": []
        },
        "dialog": {
          "$ref": "#/$defs/annotationDialog",
          "description": "Dialog content configuration used when behavior is set to dialog or detach."
        }
      }
    },
    "structureDefinition": {
      "type": "object",
      "description": "Definition for annotations rendered as structural, list, or table elements.",
      "additionalProperties": false,
      "required": ["layer"],
      "properties": {
        "layer": {
          "const": "structure",
          "description": "Declares this definition as a structural annotation."
        },
        "behavior": {
          "$ref": "#/$defs/structureBehavior",
          "description": "Rendering behavior for this structural annotation.",
          "default": "mark"
        },
        "role": {
          "$ref": "#/$defs/structureRole",
          "description": "Semantic structure role used by the text view renderer.",
          "default": "block"
        },
        "renderAs": {
          "$ref": "#/$defs/structureRenderElement",
          "description": "HTML element used to render the structural annotation.",
          "default": "div"
        },
        "placement": {
          "$ref": "#/$defs/annotationPlacement",
          "description": "Optional placement hint for structural annotations.",
          "default": "inline"
        },
        "priority": {
          "type": "integer",
          "description": "Ordering hint for nested structural annotations. Lower values wrap outside higher values."
        },
        "classes": {
          "type": "array",
          "description": "CSS utility classes added to the structural annotation element.",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "classMappings": {
          "type": "array",
          "description": "Rules that map annotation property values to CSS classes.",
          "items": {
            "$ref": "#/$defs/annotationClassMapping"
          },
          "default": []
        },
        "label": {
          "type": "array",
          "description": "Optional bindings whose resolved values may be used as a visible label for the structural annotation.",
          "items": {
            "$ref": "#/$defs/annotationValue"
          },
          "default": []
        }
      }
    },
    "annotationClassMapping": {
      "type": "object",
      "description": "Maps values from one annotation property to CSS classes.",
      "additionalProperties": false,
      "required": ["property", "mappings"],
      "properties": {
        "property": {
          "type": "string",
          "minLength": 1,
          "description": "Annotation source property containing one or more whitespace-separated values."
        },
        "mappings": {
          "type": "object",
          "description": "Maps property values to CSS utility classes.",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      }
    },
    "annotationDialog": {
      "type": "object",
      "description": "Configuration for information shown when an inline annotation is opened.",
      "additionalProperties": false,
      "properties": {
        "title": {
          "type": "string",
          "description": "Static title or translation key shown as the dialog title."
        },
        "description": {
          "type": "array",
          "description": "Values shown as dialog description text. Each value is resolved relative to the annotation node.",
          "items": {
            "$ref": "#/$defs/annotationValue"
          },
          "default": []
        },
        "externalLink": {
          "$ref": "#/$defs/annotationValue",
          "description": "Binding resolving to an external URL shown as an action."
        },
        "text": {
          "$ref": "#/$defs/binding",
          "description": "Binding resolving to text rendered inside the dialog."
        },
        "annotations": {
          "$ref": "#/$defs/binding",
          "description": "Binding resolving to annotations applied to the dialog text."
        },
        "image": {
          "$ref": "#/$defs/annotationValue",
          "description": "Binding resolving to an image URL shown inside the dialog."
        },
        "references": {
          "type": "array",
          "description": "Referenced nodes shown as navigation actions in the dialog.",
          "items": {
            "$ref": "#/$defs/annotationReference"
          },
          "default": []
        }
      }
    },
    "annotationValue": {
      "type": "object",
      "description": "Binding for a display value used by annotation labels or dialogs.",
      "additionalProperties": false,
      "required": ["path"],
      "properties": {
        "path": {
          "type": "string",
          "minLength": 1,
          "description": "RAMEN access path resolved relative to the annotation node."
        },
        "display": {
          "type": "string",
          "description": "Optional display template. The placeholder {{ value }} is replaced with the resolved value."
        },
        "valueMap": {
          "type": "object",
          "description": "Optional map replacing resolved raw values with display values before rendering.",
          "additionalProperties": true
        },
        "annotations": {
          "$ref": "#/$defs/binding",
          "description": "Optional binding resolving to annotations applied to this description value."
        }
      }
    },
    "binding": {
      "type": "object",
      "description": "Binding resolved relative to the current source object.",
      "additionalProperties": false,
      "required": ["path"],
      "properties": {
        "path": {
          "type": "string",
          "minLength": 1,
          "description": "RAMEN access path resolved relative to the current source object."
        },
        "valueMap": {
          "type": "object",
          "description": "Optional map replacing resolved raw values with display values before rendering.",
          "additionalProperties": true
        }
      }
    },
    "annotationReference": {
      "type": "object",
      "description": "Reference action shown in an annotation dialog.",
      "additionalProperties": false,
      "required": ["label", "uuid"],
      "properties": {
        "label": {
          "$ref": "#/$defs/annotationValue",
          "description": "Binding resolving to the visible reference label."
        },
        "uuid": {
          "$ref": "#/$defs/binding",
          "description": "Binding resolving to the referenced node UUID."
        },
        "icon": {
          "type": "string",
          "description": "Optional icon class shown with the reference action, for example a PrimeIcons class."
        }
      }
    },
    "annotationPlacement": {
      "type": "string",
      "description": "Placement of detached annotation output.",
      "enum": ["inline", "margin"],
      "default": "inline"
    },
    "inlineBehavior": {
      "type": "string",
      "description": "Inline annotation behavior. mark renders the annotated text in-place; dialog renders it as interactive annotation; detach removes or separates the annotated range and renders a label at the configured placement; line-break inserts a line break; hidden suppresses rendering.",
      "enum": ["mark", "dialog", "detach", "line-break", "hidden"]
    },
    "structureBehavior": {
      "type": "string",
      "description": "Structural annotation behavior. mark renders the structure; hidden suppresses rendering.",
      "enum": ["mark", "hidden"]
    },
    "inlineRenderElement": {
      "type": "string",
      "description": "Allowed HTML elements for inline annotations.",
      "enum": ["span", "abbr", "cite", "code", "em", "mark", "strong", "sub", "sup"]
    },
    "structureRenderElement": {
      "type": "string",
      "description": "Allowed HTML elements for structural annotations, including block, list, and table elements.",
      "enum": [
        "div",
        "p",
        "section",
        "article",
        "header",
        "footer",
        "address",
        "blockquote",
        "h1",
        "h2",
        "h3",
        "h4",
        "h5",
        "h6",
        "ul",
        "ol",
        "li",
        "table",
        "thead",
        "tbody",
        "tr",
        "th",
        "td"
      ]
    },
    "structureRole": {
      "type": "string",
      "description": "Semantic role used to group and render structural annotations.",
      "enum": ["block", "list", "list-item", "table", "table-section", "table-row", "table-cell"],
      "default": "block"
    },
    "unknownAnnotationStrategy": {
      "type": "string",
      "description": "Strategy for annotation types without a matching definition. ignore drops them; plain renders unstyled text; warn renders with warning semantics.",
      "enum": ["ignore", "plain", "warn"]
    },
    "invalidRangeStrategy": {
      "type": "string",
      "description": "Strategy for invalid annotation ranges. ignore drops them; clamp adjusts them to valid bounds; warn keeps warning semantics.",
      "enum": ["ignore", "clamp", "warn"]
    }
  }
}
