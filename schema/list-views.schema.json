{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ramen-schema.org/shoyu/2026.1/lists.schema.json",
  "title": "Shoyu Lists",
  "description": "Configuration for entity and collection list views, including list options, visible properties, sorting, filtering, and optional client-side value maps.",
  "$ref": "#/$defs/listViews",
  "$defs": {
    "bindingPath": {
      "type": "string",
      "description": "RAMEN path used for list display, sorting, or filtering. This is intentionally a path string rather than a full binding object to avoid expensive server-side value expansion for list queries.",
      "pattern": "^[A-Za-z0-9_.:\\[\\]*-]+$"
    },
    "identifier": {
      "type": "string",
      "description": "Stable identifier used for selecting options from initial values or query parameters.",
      "pattern": "^[A-Za-z0-9_.:-]+$"
    },
    "sortDirection": {
      "type": "string",
      "description": "Sort direction used for list sorting.",
      "enum": ["asc", "desc"]
    },
    "listIndexName": {
      "type": "string",
      "description": "Name of the configured list index used for grouping, filtering, sorting, or navigating this list option. The value should reference a key from index.config.json.",
      "pattern": "^[A-Za-z0-9_.:-]+$"
    },
    "stringMap": {
      "type": "object",
      "description": "Client-side map replacing raw list values with display labels.",
      "additionalProperties": {
        "type": "string"
      }
    },
    "property": {
      "type": "object",
      "description": "Property displayed for each list result.",
      "additionalProperties": false,
      "required": ["name"],
      "properties": {
        "name": {
          "$ref": "#/$defs/bindingPath",
          "description": "Path of the property to display for each result item."
        },
        "display": {
          "type": "string",
          "description": "Optional display template. The placeholder {{ value }} is replaced with the raw or mapped value."
        },
        "valueMap": {
          "$ref": "#/$defs/stringMap",
          "description": "Optional client-side map replacing raw property values with display labels."
        }
      }
    },
    "filterOption": {
      "type": "object",
      "description": "Filter available for a list option.",
      "additionalProperties": false,
      "required": ["label", "value"],
      "properties": {
        "icon": {
          "type": "string",
          "description": "Optional icon class shown with the filter."
        },
        "label": {
          "type": "string",
          "description": "Static label or translation key shown for the filter."
        },
        "display": {
          "type": "string",
          "description": "Filter UI display mode.",
          "enum": ["list", "range"]
        },
        "value": {
          "$ref": "#/$defs/bindingPath",
          "description": "Path used as the filter field."
        },
        "valueMap": {
          "$ref": "#/$defs/stringMap",
          "description": "Optional client-side map replacing raw filter values with display labels."
        }
      }
    },
    "sortField": {
      "type": "object",
      "description": "Single field used as part of a sort option. Multiple sort fields are applied in array order.",
      "additionalProperties": false,
      "required": ["field"],
      "properties": {
        "field": {
          "$ref": "#/$defs/bindingPath",
          "description": "Path used as the sort field."
        },
        "direction": {
          "$ref": "#/$defs/sortDirection",
          "description": "Direction used for this specific sort field. If omitted, the sort option group's default direction is used."
        }
      }
    },
    "sortValue": {
      "description": "Sort definition. A string sorts by one binding path. An array sorts by multiple fields in order.",
      "oneOf": [
        {
          "$ref": "#/$defs/bindingPath"
        },
        {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/sortField"
          }
        }
      ]
    },
    "sortOption": {
      "type": "object",
      "description": "Sort option available for a list option. The value may be a single binding path or an ordered list of sort fields.",
      "additionalProperties": false,
      "required": ["identifier", "label", "value"],
      "properties": {
        "identifier": {
          "$ref": "#/$defs/identifier",
          "description": "Stable identifier used to select this sort option from initial values or query parameters."
        },
        "icon": {
          "type": "string",
          "description": "Optional icon class shown with the sort option."
        },
        "label": {
          "type": "string",
          "description": "Static label or translation key shown for the sort option."
        },
        "value": {
          "$ref": "#/$defs/sortValue",
          "description": "Single path or ordered list of fields used for sorting."
        }
      }
    },
    "sortOptionGroup": {
      "type": "object",
      "description": "Configured sort options and their default direction.",
      "additionalProperties": false,
      "required": ["initial", "options", "direction"],
      "properties": {
        "initial": {
          "$ref": "#/$defs/identifier",
          "description": "Identifier of the sort option selected by default."
        },
        "options": {
          "type": "array",
          "description": "Available sort options.",
          "items": {
            "$ref": "#/$defs/sortOption"
          }
        },
        "direction": {
          "$ref": "#/$defs/sortDirection",
          "description": "Default direction applied to selected single-field sort options and to sort fields without their own direction."
        }
      }
    },
    "listOption": {
      "type": "object",
      "description": "Selectable collection list option.",
      "additionalProperties": false,
      "required": ["label", "value"],
      "properties": {
        "icon": {
          "type": "string",
          "description": "Optional icon class shown with the list option."
        },
        "label": {
          "type": "string",
          "description": "Static label or translation key shown with the list option."
        },
        "value": {
          "$ref": "#/$defs/bindingPath",
          "description": "Internal value identifying this list option."
        },
        "properties": {
          "type": "array",
          "description": "Properties displayed for results in this list option.",
          "items": {
            "$ref": "#/$defs/property"
          }
        },
        "sort": {
          "$ref": "#/$defs/sortOptionGroup",
          "description": "Sort options available for this list option."
        },
        "filters": {
          "type": "array",
          "description": "Filters available for this list option.",
          "items": {
            "$ref": "#/$defs/filterOption"
          }
        }
      }
    },
    "entityOption": {
      "type": "object",
      "description": "Selectable entity list option.",
      "additionalProperties": false,
      "required": ["label", "value"],
      "properties": {
        "icon": {
          "type": "string",
          "description": "Optional icon class shown with the entity option."
        },
        "label": {
          "type": "string",
          "description": "Static label or translation key shown with the entity option."
        },
        "value": {
          "$ref": "#/$defs/bindingPath",
          "description": "Internal value identifying this entity option."
        },
        "properties": {
          "type": "array",
          "description": "Properties displayed for results in this entity option.",
          "items": {
            "$ref": "#/$defs/property"
          }
        },
        "index": {
          "$ref": "#/$defs/listIndexName",
          "description": "Optional configured list index used for this entity option. References a key from index.config.json."
        }
      }
    },
    "entityListView": {
      "type": "object",
      "description": "Configuration for entity list navigation.",
      "additionalProperties": false,
      "required": ["initial", "options"],
      "properties": {
        "initial": {
          "$ref": "#/$defs/bindingPath",
          "description": "Value of the entity option selected by default."
        },
        "options": {
          "type": "array",
          "description": "Available entity list options.",
          "items": {
            "$ref": "#/$defs/entityOption"
          }
        },
        "properties": {
          "type": "array",
          "description": "Default properties displayed for entity results unless overridden by an option.",
          "items": {
            "$ref": "#/$defs/property"
          }
        }
      }
    },
    "collectionListView": {
      "type": "object",
      "description": "Configuration for collection list navigation.",
      "additionalProperties": false,
      "required": ["initial", "options"],
      "properties": {
        "initial": {
          "$ref": "#/$defs/bindingPath",
          "description": "Value of the collection option selected by default."
        },
        "options": {
          "type": "array",
          "description": "Available collection list options.",
          "items": {
            "$ref": "#/$defs/listOption"
          }
        },
        "properties": {
          "type": "array",
          "description": "Default properties displayed for collection results unless overridden by an option.",
          "items": {
            "$ref": "#/$defs/property"
          }
        }
      }
    },
    "listViews": {
      "type": "object",
      "description": "Top-level list view configuration.",
      "additionalProperties": false,
      "required": ["entities", "collections"],
      "properties": {
        "entities": {
          "$ref": "#/$defs/entityListView",
          "description": "Entity list view configuration."
        },
        "collections": {
          "$ref": "#/$defs/collectionListView",
          "description": "Collection list view configuration."
        }
      }
    }
  }
}
